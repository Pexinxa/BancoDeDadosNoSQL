from django.contrib import admin

# Todos os modelos customizados (Produto, Categoria, Contato, Compra, Avaliação)
# são gerenciados pelo Firebase Firestore e não pelo Django Admin ORM.
# O painel admin do Django está disponível apenas para o gerenciamento de usuários (User/auth).
